# GitHub Personal Access Token (PAT)
# Needs `repo` scope to clone private repos and open PRs.
# For safety during testing, use a fine-grained PAT scoped ONLY to your specific test repository.
GITHUB_TOKEN=your_fine_grained_pat_here
